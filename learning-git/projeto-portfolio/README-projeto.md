# Projeto Portfolio — Laboratório Git

> Use este diretório como **playground**. Não é para commitar no StudyNotes — copie para `/tmp/portfolio` e brinque.

```bash
cp -r learning-git/projeto-portfolio /tmp/portfolio && cd /tmp/portfolio
git init && git add . && git commit -m "feat: initial portfolio"
# siga os caps. 03→12
```
